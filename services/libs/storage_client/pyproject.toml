[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "storage_client"
version = "0.0.1"
description = "Shared MinIO/S3 client for local dev (editable install)"
authors = [{name="you"}]
dependencies = ["boto3", "python-dotenv"]
